name: build-apk

on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4

      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: app/package-lock.json

      - name: npm install
        run: npm ci
        working-directory: app

      - name: build web + capacitor android
        working-directory: app
        env:
          # Stable ngrok dev target. The tunnel forwards to Vite :5173, which
          # proxies /api to the local PHP API on :8081.
          VITE_API_BASE: https://stunning-unclamped-hydrated.ngrok-free.dev/api
        run: |
          python3 -m pip install --user pillow
          npm run build
          npx cap add android
          npx cap sync android
          python3 scripts/gen-icons.py android/app/src/main/res

      - name: setup java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: setup android sdk
        uses: android-actions/setup-android@v3

      - name: install sdk components
        run: |
          yes | sdkmanager --licenses > /dev/null || true
          sdkmanager "platforms;android-35" "build-tools;35.0.0" || true

      - name: build apk (debug)
        working-directory: app/android
        run: ./gradlew assembleDebug

      - name: release to github
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          APK=app/android/app/build/outputs/apk/debug/app-debug.apk
          if [[ "${{ github.ref }}" == refs/tags/* ]]; then
            gh release create "${{ github.ref_name }}" "$APK" \
              --title "Pondok Huda ${{ github.ref_name }}" \
              --notes "Rilis dari tag ${{ github.ref_name }}." \
              --repo "${{ github.repository }}"
          else
            gh release delete dev --repo "${{ github.repository }}" --yes 2>/dev/null || true
            gh release create dev "$APK" \
              --title "Pondok Huda (build otomatis)" \
              --notes "Build otomatis dari main. commit: ${{ github.sha }}" \
              --repo "${{ github.repository }}"
          fi
